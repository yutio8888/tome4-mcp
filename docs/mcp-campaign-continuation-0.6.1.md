# ToME MCP 0.6.1：护送成功并推进至 Kor’Pul 第 2 层

2026-09-15。使用正式 Bridge / Python **0.6.1**、显式 **protocol 2**，从本人 `campaign-play-v060-02` 的 **Lv5 World of Eyal `(28,13)` 七文件存档**复制到新的隔离目录继续。

本轮成功按 MCP 地图进入 Kor’Pul，完成受伤先知护送、领取意志 +5，经历 117 次原生日志确认的击杀，自然升至 6 级，并下到 Kor’Pul 第 2 层。现已在第 2 层已清理的入口房间 `(10,16)` 满生命、满体力原生保存，全部游戏进程已关闭。**整场战役仍未完成。**

上轮世界地图全部未知的问题在本次实玩中已修复：初始半径 12 的 625 格包含 **39 可见、39 已知、586 未知**，Trollmire `(28,13)` 和 Kor’Pul `(24,14)` 入口均正确可见。后续移动与导航使用 MCP 返回的当前感知和已知地形。

![原生游戏画面：6 级角色在 Kor’Pul 第 2 层的安全入口房间，生命 234/234](mcp-campaign-continuation-0.6.1.png)

本轮会话目录为 `tmp/tome-mcp-validation/sessions/campaign-play-v061-01`。来源链延续 [0.6.0 报告](mcp-campaign-continuation-0.6.0.md)，没有重新从 Lv3 打 Prox，也没有使用开发者验收角色。启动时逐项核对位置、等级、经验、资源、属性、成长点、状态、装备和背包，记录在 `load-verification.json`。

| 实战流程 | 结果与证据 |
|---|---|
| 世界地图导航 | 正确观测两处入口，经 MCP 移动到 Kor’Pul；修复后的世界地图支持了实际出行 |
| `play061-00006` 进入 Kor’Pul | 已发生换层，返回 `needs_input / scene_changed`，触发护送任务的 QuestPopup 与 Chat；未重放换层 |
| 护送 Polewyn | 保护先知穿过鼠群、蛇和其他敌人；最后可见生命 35.85/106.35，原生日志确认护送成功 |
| `play061-00224` 护送完成 | 移动已经发生，奖励界面触发 `needs_input / dialog`；选择意志 +5，并关闭后续告别页 |
| 6 级成长 | 经 MCP 投入 3 点力量，Stunning Blow 升至 4、Weapons Mastery 升至 2；所有成长点用完 |
| 两张自然发现的日记 | `00029`、`00929` 拾取后均返回 `dialog.notice / LorePopup`；回答当前 Close 后正常完成，无需游戏按键 |
| `play061-00909` 下楼 | 第 1 层楼梯处实际进入第 2 层，`completed / level_changed`、`level_changed=true`、执行权释放；随后重新取得控制 |
| 第 2 层入口 | 清除了蠕虫群和骷髅等近处敌人，阅读日记，关闭持续技能；最后原生休息 34 回合恢复至满状态 |

**仍然存在的 Bridge 限制是原生 Chat。** 本轮进一步自然覆盖了“护送完成 → 奖励选择 → 告别页”，MCP 可以报告对话正文，但未提供可回答的 Chat 选项。奖励选项必须从实际原生画面读取。

在首次进入时，人工按 Escape 关闭上层任务通知，再按 `a` 选择保护先知；护送完成时，按 Escape 关闭完成通知、按 `b` 选择可见的“意志 +5”、再按 `a` 回答告别页。合计 **5 次游戏界面按键**，均有 `ui-interventions.jsonl` 与截图记录。两条原命令没有重放，处理结束后其历史状态仍为 `needs_input`，但 `execution_released=true`；新的 observe 才用于确认当前已恢复 ready。

建议后续适配真实 Chat 选项、选项翻页与后续对话，并保留已经发生的移动或换层及原命令所有权。相关截图是 `escort-chat-full.png`、`escort-reward-popup.png`、`escort-reward-options.png`、`escort-reward-thanks.png`。这属于已知 Chat 缺口的新实战覆盖，本轮没有确认新的 Bridge 核心回归。

此外，本轮保留了三类操作或脚本问题，未归咎于 Bridge：

- `play061-00003` 在入口之外请求换层，原生正常拒绝、耗能 0。之后移动到真实入口才换层。
- `play061-00022` 尝试使用 Prox’s Lucky Halfling Foot，原生返回 false、耗能 0。已知物品源码中的 `use_power` 是隐藏的空操作；`activation.present` 描述存储结构，不能当作使用成功的保证。
- `play061-00274` 拾取请求漏传 `item_id`，被 Python 参数校验拒绝；查询确认原生没有该命令。续玩脚本却留下了本地 pending，因而先原生保存，再替换外部控制器与 stdio 服务。游戏进程未重启，session、位置、角色状态和 `world_tick=38556` 保持一致。新控制器先校验参数，并以新命令 `00275` 正常拾取板甲。原记录未覆盖，后续记录在 `controller-resume/`。

一个辅助连接尝试发生在旧 TCP 客户端尚未关闭时，得到预期的单客户端连接拒绝；它没有提交游戏动作。两段控制记录、该失败尝试和切换前的八文件原生检查点均保留。

导航也暴露了调用方策略问题：关闭的门同时具有 `blocked=true` 与 `can_open=true`，原续玩脚本错误地排除了这类门；改用已知地形规划并让原生移动开门后继续前进。护送时必须给先知让出通道，休息前也应关闭仍在耗血的 Berserker Rage。这些经历解释了本轮部分绕路和重复休息，不能用本轮动作数估计 MCP 的必要操作量。

| 统计 | 本轮结果 |
|---|---|
| 动作请求 | 931 个：930 个进入原生命令系统，1 个在 Python 参数校验处被拒绝 |
| 原生命令最终状态 | 926 completed；2 次正常原生拒绝；2 次 Chat 人工接管，均最终释放执行权 |
| MCP 交互回答 | 42 次 applied：31 次网格目标、9 次方向目标、2 次 LorePopup |
| 战斗 | 117 次玩家击杀；MCP 快照中最低观测生命 117.7347，结束为满生命 |
| 可见日志 | 合并两段记录后包含连续的 1,684 个 cursor：1,040 append、644 remove |
| 日志游标恢复 | 新控制器从 cursor 0 请求时收到一次 gap，桥保留起点为 119；旧控制器已记录此前内容，合并证据无缺失 |
| 运行错误 | 主控制记录含 1 次参数校验错误及相应 unknown_command 查询；恢复后的控制记录无工具错误；无 Lua 错误栈 |
| Battle Companion | 所有角色快照均为 idle / actions 0 |
| 最终角色 | Lv6，力量 27、体质 19、意志 10+5；经验 296.54/346；无剩余成长点 |
| 最终位置与资源 | Kor’Pul 2 `(10,16)`；生命 234/234，体力 127.5/127.5；无状态效果、无冷却、无可见敌人、无原生 pending |

回答去重以 `(command_id, response_id)` 核对，42 对均唯一。接续脚本的计数起点少计了一次，导致文本 `play061-answer-00009` 出现在不同命令各一次；没有在同一命令上重放回答，全部 receipt 均为 applied。

正式包、永久副本和实际运行包 SHA-256 均为 `7df97a9121ac75470436f48c136dbe366f12b083688f0c508d1c86e367e7af0e`，34 个生产文件逐字节一致。两个控制器使用同一份冻结 Python 源码；项目版本、venv 安装元数据及冻结元数据均核对为 0.6.1，MCP SDK 为 2.2.0。本轮未修改生产实现，没有 gameplay fixture、临时给予角色能力或物品、改属性、改经验或隐藏地图查询。

结束后复核 **4 份历史报告、其中 117 项证据引用、25 个历史存档文件，以及 107 项 0.6.1 交付基线文件**，全部一致。最终原生保存的 game/world 有效性检查及存档压缩包 CRC 均通过；两个控制脚本退出码均为 0，隔离引擎和 Xvfb 已退出。最终存档没有再启动读档测试，启动时对上轮来源的加载验证另有记录。

后续续玩请复制本轮 **Lv6 Kor’Pul 2 `(10,16)` 八文件存档**：`tmp/tome-mcp-validation/sessions/campaign-play-v061-01/home/.t-engine/4.0/tome/save`。原控制器末尾遗留的本地 pending 是已被参数校验拒绝的 `00274`，不代表保存时仍有原生动作等待；实际最终状态与记录在 `controller-resume/play-summary.json`。

[结构化报告](mcp-campaign-continuation-0.6.1.json)包含命令、交互、失败分类、来源校验、最终存档及证据哈希。按用户既有要求，已通过 Paseo 向原 MCP 开发 agent 成功投递 Chat 奖励及后续页缺口的功能摘要。详细消息被自动审批拒绝，最终发送的摘要不包含工作区路径或原始记录；完整证据保存在本地。实际投递内容与两次请求回执记在报告的 `handoff` 字段及 `paseo-feedback-receipt.json`。
